{{!-- para pegar os erros que vieram do envio do formulario de categorias --}}
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h1 class="mb-4 h2">Nova categoria</h1>

<form action="/admin/categorias/nova" method="POST">
    <label for="nome">Nome:</label>
    <input type="text" class="form-control mb-3" name="nome" id="nome" placeholder="Digite o nome que quer cadastrar">
    <label for="slut">Slug:</label>
    <input type="text" class="form-control mb-3" name="slug" id="slug" placeholder="slug da categoria">
    <button type="submit" class="btn btn-danger mt-4">Criar categoria</button>
</form>