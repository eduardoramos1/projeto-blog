{{!-- para pegar os erros que vieram do envio do formulario de categorias --}}
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<h1 class="mb-4 h2">Editar categoria</h1>

<form action="/admin/categorias/edit" method="POST">
    <input type="hidden" name="id" value="{{categoria._id}}">
    <label for="nome">Nome:</label>
    <input type="text" class="form-control mb-3" name="nome" id="nome" placeholder="Digite o nome que quer cadastrar" value="{{categoria.nome}}">
    <label for="slut">Slug:</label>
    <input type="text" class="form-control mb-3" name="slug" id="slug" placeholder="slug da categoria" 
    value="{{categoria.slug}}"  >
    <button type="submit" class="btn btn-secondary mt-4">Editar categoria</button>
</form>